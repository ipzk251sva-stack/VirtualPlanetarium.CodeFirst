@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Віртуальний Планетарій</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <style>
        body {
            background-color: #050505;
            color: #e0e0e0;
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .navbar {
            background-color: rgba(10, 10, 20, 0.95) !important;
            border-bottom: 1px solid #333;
            box-shadow: 0 0 15px rgba(0, 210, 255, 0.1);
        }

        .navbar-brand {
            color: #00d2ff !important;
            font-weight: bold;
            font-size: 1.5rem;
            text-shadow: 0 0 10px rgba(0, 210, 255, 0.5);
        }

        .nav-link {
            color: #aaa !important;
            transition: 0.3s;
            margin: 0 5px;
        }

            .nav-link:hover {
                color: #fff !important;
                text-shadow: 0 0 5px #fff;
            }

        main {
            flex: 1;
            padding-top: 0;
        }

        footer {
            background-color: #0a0a0a;
            color: #666;
            text-align: center;
            padding: 10px 0;
            border-top: 1px solid #222;
            margin-top: auto;
            position: relative;
            z-index: 100;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-md navbar-dark">
            <div class="container-fluid">
                <a class="navbar-brand ms-3" asp-area="" asp-controller="CelestialObjects" asp-action="Map">
                    🚀 Віртуальний Планетарій
                </a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-end me-3">
                    <ul class="navbar-nav flex-grow-1">

                        <li class="nav-item">
                            <a class="nav-link" asp-controller="CelestialObjects" asp-action="Index">📜 Каталог</a>
                        </li>

                    </ul>

                    <ul class="navbar-nav">
                        @if (SignInManager.IsSignedIn(User))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-info" asp-controller="Home" asp-action="Profile">
                                    👤 @User.Identity?.Name
                                </a>
                            </li>
                            <li class="nav-item">
                                <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Map", "CelestialObjects", new { area = "" })">
                                    <button type="submit" class="nav-link btn btn-link text-white" style="text-decoration: none;">Вихід</button>
                                </form>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item"><a class="nav-link" asp-area="Identity" asp-page="/Account/Register">Реєстрація</a></li>
                            <li class="nav-item"><a class="nav-link text-warning" asp-area="Identity" asp-page="/Account/Login">Вхід ➜</a></li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container-fluid p-0">
        <main role="main">
            @RenderBody()
        </main>
    </div>

    <footer>
        <div class="container">
            &copy; 2026 - Віртуальний Планетарій
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>