@model IEnumerable<VirtualPlanetarium.CodeFirst.Models.CelestialObject>

@{
    ViewData["Title"] = "Каталог";
    Layout = "_Layout";
}

<div class="container mt-5 pt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-info fw-bold">🔭 Каталог об'єктів</h1>

        @if (User.IsInRole("Administrator"))
        {
            <a asp-action="Create" class="btn btn-success btn-lg shadow">
                ➕ Додати новий об'єкт
            </a>
        }
    </div>

    <div class="row g-4 pb-5">
        @foreach (var item in Model)
        {
            <div class="col-md-6 col-lg-4">
                <div class="card h-100 bg-dark text-white border-secondary shadow-sm" style="border: 1px solid #333;">

                    <div class="card-header border-secondary d-flex justify-content-between align-items-center">
                        <h4 class="card-title text-info m-0">@item.Name</h4>
                        <span class="badge bg-primary">@item.Type?.Name</span>
                    </div>

                    <div class="card-body">
                        <p class="card-text text-muted small">
                            @(item.Description != null && item.Description.Length > 80
                                                    ? item.Description.Substring(0, 80) + "..."
                                                    : item.Description)
                        </p>
                        <div class="d-flex justify-content-between mt-3 text-small">
                            <span class="text-warning">⭐ @item.SpectralClass</span>
                            <span class="text-light">📍 @item.Group?.Name</span>
                        </div>
                    </div>

                    <div class="card-footer border-secondary bg-transparent d-flex gap-2">
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info flex-grow-1">
                            📖 Інфо
                        </a>

                        @if (User.IsInRole("Administrator"))
                        {
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning" title="Редагувати">
                                ✏
                            </a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger" title="Видалити">
                                🗑
                            </a>
                        }
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<style>
    .card:hover {
        transform: translateY(-5px);
        transition: 0.3s;
        border-color: #00d2ff !important;
        box-shadow: 0 5px 15px rgba(0, 210, 255, 0.2);
    }
</style>